language: java

jdk:
  - oraclejdk7
  - openjdk7
  - openjdk6

services:
  - memcache

env:
  - PLAY_VERSION=2.0.1

before_script:
  - wget http://download.playframework.org/releases/play-${PLAY_VERSION}.zip
  - unzip -q play-${PLAY_VERSION}.zip

script:
  - sbt test

